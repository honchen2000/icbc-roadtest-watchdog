# Normalize line endings: LF in the repo, OS-appropriate on checkout.
* text=auto eol=lf

# Windows-only scripts (if any) keep CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.icns binary
